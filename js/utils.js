function calculateContentWidth(svgWidth, margin) {
   return svgWidth - margin.left - margin.right;
}

function calculateContentHeight(svgHeight, margin) {
  return svgHeight - margin.top - margin.bottom;
}